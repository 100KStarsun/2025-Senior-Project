# Compiled class file
*.class

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Package Files #
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
replay_pid*

# local.properties should be unique to each system, we need to keep it out of the repo
local.properties

# the gradle wrapper jar should be included to ensure we are all using the same version, but the rest of .gradle shouldnt get committed
./gradle
!./gradle/wrapper/gradle-wrapper.jar

# ignore the build folder (i believe this will get bloated quickly)
./app/build
